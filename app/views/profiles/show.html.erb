    <div>
        <div>
            <div>
                <% if @profile.cover_photo.attached? %>
                    <%= image_tag(@profile.cover_photo , class: "w-full h-15" )%>
                <% end %>
            </div>
            <div>
                <% if @profile.profile_photo.attached? %>
                    <%= image_tag(@profile.profile_photo, class: "w-10 h-10 rounded-full") %>
                <% end %>
                <%= button_to "Edit Profile", edit_profile_path(@profile), method: :get, class: "mt-4 bg-blue-500 text-white py-2 rounded" %>
            </div>
        </div>
    </div>
    <div>
        <h1>Details about profile owner go here eg username, follower count, bio, website</h1>
        <div>
        <div>
            <p><%= @profile.user.username %><b>@<%= @profile.user.username %></b></p>
            <% if current_user != @profile_owner %>
                <% if current_user.following?(@profile_owner)%>
                    <% relationship = current_user.relationship_with(@profile_owner)%>
                    <%= button_to "Unfollow User", destroy_relationship_path, method: :delete, params: {id: relationship.id, followed_id: @profile_owner.id}, class: "mt-4 bg-blue-500 text-white py-2 rounded" %>
                <% else %>
                    <%= button_to "Follow User", create_relationship_path, method: :post, params: {followed_id: @profile_owner.id}, class: "mt-4 bg-blue-500 text-white py-2 rounded" %>
                <% end %>
            <% end %>
        </div>
        <div>
            <div><%=@profile.user.followers.count%><%= button_to "Followers",followers_user_path(@profile.user), method: :get, class: "mt-4 bg-blue-500 text-white py-2 rounded"  %></div>
            <div><%=@profile.user.following.count%><%= button_to "Following", following_user_path(@profile.user), method: :get, class: "mt-4 bg-blue-500 text-white py-2 rounded"  %></div>
        </div>
        
        <div>
            <p>Bio:<%= @profile.bio %></p>
        </div>
        <div>
            <p>Location:<%= @profile.location %></p>
        </div>
        <div>
            <p>Website:<%= @profile.website %></p>
        </div>
    </div>
    </div>
    <div>
        <h1>All Tweets, Retweets and Quote tweets go here</h1>
    <h2>Tweets</h2>
    <div class="border-t-2 border-b-2">
        <% @profile.user.tweets.each do |tweet| %>
            <div>
            <%= tweet.body %>
            <%= link_to "View tweet", tweet_path(tweet)%>
            </div>
            <div class=" flex">
            <div class="flex">
                <% if current_user.likes.exists?(likeable_id: tweet.id, likeable_type: tweet.class.name)%>
                    <%= button_to like_path(tweet.likes.find_by(user_id: current_user.id)), method: :delete do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full fill-red-500 cursor-pointer" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                    <% end %>
                    <p><%= tweet.likes.count %> Likes</p>
                <% else %>
                      <%= button_to likes_path(tweet), params: {like: {likeable_id: tweet.id, likeable_type: tweet.class.name}}, method: :post do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                      <% end %>
                      <p><%= tweet.likes.count %> Likes </p>
                <% end %>
            </div>
            <div class="flex hover:text-blue-500 cursor-pointer">
                <% if current_user.bookmarks.exists?(bookmarkable_id: tweet.id ,bookmarkable_type: "Tweet", user_id: current_user.id)%>
                    <%= button_to bookmark_path(tweet.bookmarks.find_by(user_id: current_user.id)), method: :delete do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full fill-blue-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=tweet.bookmarks.count%> Bookmarks</p>
                <% else %>
                     <%= button_to bookmarks_path(tweet), params: {bookmark: {bookmarkable_type: tweet.class.name, bookmarkable_id: tweet.id}}, method: :post do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=tweet.bookmarks.count%> Bookmarks</p>
                <% end %>
            </div>
        </div>
        <% end %>
        <%= link_to "Create new tweet", new_tweet_path %>
    </div> 
    <h3>Retweets</h3>
    <% if @profile.user.id == current_user.id %>
        <% if current_user.retweets.any?%>
            <div class="border-t-2 border-b-2">
                <% current_user.retweets.each do |retweet| %>
                    <p>You Retweeted</p>
                    <%= image_tag("retweet-icon.svg", class: "inline-block h-6 w-6 hover:fill-blue-500")%>
                    <%= retweet.tweet.user.username %>
                    <%= retweet.tweet.body%>
        <div class=" flex">
            <div class="flex">
                <% if current_user.likes.exists?(likeable_id: retweet.id, likeable_type: retweet.class.name)%>
                    <%= button_to like_path(retweet.likes.find_by(user_id: current_user.id)), method: :delete do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full fill-red-500 cursor-pointer" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                    <% end %>
                    <p><%= retweet.likes.count %> Likes</p>
                <% else %>
                      <%= button_to likes_path(retweet), params: {like: {likeable_id: retweet.id, likeable_type: retweet.class.name}}, method: :post do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                      <% end %>
                      <p><%= retweet.likes.count %> Likes </p>
                <% end %>
            </div>
            <div class="flex hover:text-blue-500 cursor-pointer">
                <% if current_user.bookmarks.exists?(bookmarkable_id: retweet.id ,bookmarkable_type: "Retweet", user_id: current_user.id)%>
                    <%= button_to bookmark_path(retweet.bookmarks.find_by(user_id: current_user.id)), method: :delete do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full fill-blue-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=retweet.bookmarks.count%> Bookmarks</p>
                <% else %>
                     <%= button_to bookmarks_path(retweet), params: {bookmark: {bookmarkable_type: retweet.class.name, bookmarkable_id: retweet.id}}, method: :post do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=retweet.bookmarks.count%> Bookmarks</p>
                <% end %>
            </div>
        </div>
                    <% if retweet.likes.any? %>
                        <div>
                            <p><%= retweet.likes.count %> Likes</p>
                            <%= button_to "Unlike retweet", like_path(retweet.likes.find_by(user_id: current_user.id)), method: :delete %>
                        </div>
                    <% else %>
                        <%= button_to "Like Retweet", likes_path(retweet), params: {like: {likeable_id: retweet.id, likeable_type: retweet.class.name }}%>
                    <% end %>
                    <% if  current_user.bookmarks.exists?(bookmarkable_id: retweet.id ,bookmarkable_type: "Retweet", user_id: current_user.id ) %>
                        <%= button_to "Unbookmark", bookmark_path(retweet.bookmarks.find_by(user_id: current_user.id)), method: :delete %>
                    <% else %>
                        <%= button_to "Bookmark", bookmarks_path(retweet), params: {bookmark: {bookmarkable_type: retweet.class.name, bookmarkable_id: retweet.id}}, method: :post %>
                    <% end %>
                <% end %>
            </div>
        <% end %>
    <% else %>
        <% if @profile.user.retweets.any? %>
            <div class="border-t-2 border-b-2">
                <% @profile.user.retweets.each do |retweet| %>
                    <p><%= @profile.user.username %> Retweeted</p>
                    <%= image_tag("retweet-icon.svg", class: "inline-block h-6 w-6 hover:fill-blue-500")%>
                    <%= retweet.tweet.user.username %>
                    <%= retweet.tweet.body%>
                    <div class=" flex">
            <div class="flex">
                <% if current_user.likes.exists?(likeable_id: retweet, likeable_type: retweet.class.name)%>
                    <%= button_to like_path(retweet.likes.find_by(user_id: current_user.id)), method: :delete do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full fill-red-500 cursor-pointer" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                    <% end %>
                    <p><%= retweet.likes.count %> Likes</p>
                <% else %>
                      <%= button_to likes_path(retweet), params: {like: {likeable_id: retweet.id, likeable_type: retweet.class.name}}, method: :post do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                      <% end %>
                      <p><%= retweet.likes.count %> Likes </p>
                <% end %>
            </div>
            <div class="flex hover:text-blue-500 cursor-pointer">
                <% if current_user.bookmarks.exists?(bookmarkable_id: retweet.id ,bookmarkable_type: "Retweet", user_id: current_user.id)%>
                    <%= button_to bookmark_path(retweet.bookmarks.find_by(user_id: current_user.id)), method: :delete do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full fill-blue-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=retweet.bookmarks.count%> Bookmarks</p>
                <% else %>
                     <%= button_to bookmarks_path(retweet), params: {bookmark: {bookmarkable_type: retweet.class.name, bookmarkable_id: retweet.id}}, method: :post do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=retweet.bookmarks.count%> Bookmarks</p>
                <% end %>
            </div>
        </div>
                    <% if retweet.likes.any? %>
                        <div>
                            <p><%= retweet.likes.count %> Likes</p>
                            <%= button_to "Unlike retweet", like_path(retweet.likes.find_by(user_id: current_user.id)), method: :delete %>
                        </div>
                    <% else %>
                        <%= button_to "Like Retweet", likes_path(retweet), params: {like: {likeable_id: retweet.id, likeable_type: retweet.class.name }}%>
                    <% end %>
                <% end %>
            </div>
        <% end %>
    <% end %>
    <h3>Qoute tweets</h3>
    <% if @profile.user.quotes.any? %>
        <div <% @profile.user.quotes.each do |quote| %>
            <b><em><%= quote.user.username %></em></b>
            <b><em><%= quote.body %></em></b>
            <div>
                <%= quote.tweet.user.username %>
                <%= quote.tweet.body%>
                <div class=" flex">
            <div class="flex">
                <% if current_user.likes.exists?(likeable_id: quote, likeable_type: quote.class.name)%>
                    <%= button_to like_path(quote.likes.find_by(user_id: current_user.id)), method: :delete do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full fill-red-500 cursor-pointer" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                    <% end %>
                    <p><%= quote.likes.count %> Likes</p>
                <% else %>
                      <%= button_to likes_path(quote), params: {like: {likeable_id: quote.id, likeable_type: quote.class.name}}, method: :post do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                      <% end %>
                      <p><%= quote.likes.count %> Likes </p>
                <% end %>
            </div>
            <div class="flex hover:text-blue-500 cursor-pointer">
                <% if current_user.bookmarks.exists?(bookmarkable_id: quote.id ,bookmarkable_type: "Quote", user_id: current_user.id)%>
                    <%= button_to bookmark_path(quote.bookmarks.find_by(user_id: current_user.id)), method: :delete do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full fill-blue-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=quote.bookmarks.count%> Bookmarks</p>
                <% else %>
                     <%= button_to bookmarks_path(quote), params: {bookmark: {bookmarkable_type: quote.class.name, bookmarkable_id: quote.id}}, method: :post do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=quote.bookmarks.count%> Bookmarks</p>
                <% end %>
            </div>
        </div>
                <% if quote.likes.any? %>
                    <div>
                        <p><%= quote.likes.count%> Likes</p>
                        <%= button_to "Unlike Quote", like_path(quote.likes.find_by(user_id: current_user.id)), method: :delete%>
                    </div>
                <% else %>
                    <%= button_to "Like Quote", likes_path(quote), params: {like: {likeable_type: quote.class.name, likeable_id: quote.id }}, method: :post %> 
                <% end %>
                <% if current_user.bookmarks.exists?(bookmarkable_id: quote.id,bookmarkable_type: "Quote",user_id: current_user.id) %>
                    <%= button_to "Unbookmark Quote", bookmark_path(quote.bookmarks.find_by(user_id: current_user.id)), method: :delete %>
                <% else %>
                    <%= button_to "Bookmark Quote", bookmarks_path(quote), params: {bookmark: {bookmarkable_type: quote.class.name, bookmarkable_id:quote.id}}, method: :post%>
                <% end %>
            </div>
        <% end %>>
            <% @profile.user.quotes.each do |quote| %>
            <b><em><%= quote.user.username %></em></b>
            <b><em><%= quote.body %></em></b>
            <div>
                <%= quote.tweet.user.username %>
                <%= quote.tweet.body%>
                <div class=" flex">
            <div class="flex">
                <% if current_user.likes.exists?(likeable_id: quote, likeable_type: quote.class.name)%>
                    <%= button_to like_path(quote.likes.find_by(user_id: current_user.id)), method: :delete do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full fill-red-500 cursor-pointer" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                    <% end %>
                    <p><%= quote.likes.count %> Likes</p>
                <% else %>
                      <%= button_to likes_path(quote), params: {like: {likeable_id: quote.id, likeable_type: quote.class.name}}, method: :post do %>
                        <button>
                          <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                          <path stroke-linecap="round" stroke-linejoin="round" d="M21 8.25c0-2.485-2.099-4.5-4.688-4.5-1.935 0-3.597 1.126-4.312 2.733-.715-1.607-2.377-2.733-4.313-2.733C5.1 3.75 3 5.765 3 8.25c0 7.22 9 12 9 12s9-4.78 9-12Z" />
                          </svg>
                        </button>
                      <% end %>
                      <p><%= quote.likes.count %> Likes </p>
                <% end %>
            </div>
            <div class="flex hover:text-blue-500 cursor-pointer">
                <% if current_user.bookmarks.exists?(bookmarkable_id: quote.id ,bookmarkable_type: "Quote", user_id: current_user.id)%>
                    <%= button_to bookmark_path(tweet.bookmarks.find_by(user_id: current_user.id)), method: :delete do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full fill-blue-500" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=tweet.bookmarks.count%> Bookmarks</p>
                <% else %>
                     <%= button_to bookmarks_path(quote), params: {bookmark: {bookmarkable_type: quote.class.name, bookmarkable_id: quote.id}}, method: :post do %>
                      <button>
                        <svg class="w-5 h-5 rounded-full" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="size-6">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M17.593 3.322c1.1.128 1.907 1.077 1.907 2.185V21L12 17.25 4.5 21V5.507c0-1.108.806-2.057 1.907-2.185a48.507 48.507 0 0 1 11.186 0Z" />
                        </svg>
                      </button>
                    <% end %>
                    <p><%=quote.bookmarks.count%> Bookmarks</p>
                <% end %>
            </div>
        </div>
                <% if quote.likes.any? %>
                    <div>
                        <p><%= quote.likes.count%> Likes</p>
                        <%= button_to "Unlike Quote", like_path(quote.likes.find_by(user_id: current_user.id)), method: :delete%>
                    </div>
                <% else %>
                    <%= button_to "Like Quote", likes_path(quote), params: {like: {likeable_type: quote.class.name, likeable_id: quote.id }}, method: :post %> 
                <% end %>
                <% if current_user.bookmarks.exists?(bookmarkable_id: quote.id,bookmarkable_type: "Quote",user_id: current_user.id) %>
                    <%= button_to "Unbookmark Quote", bookmark_path(quote.bookmarks.find_by(user_id: current_user.id)), method: :delete %>
                <% else %>
                    <%= button_to "Bookmark Quote", bookmarks_path(quote), params: {bookmark: {bookmarkable_type: quote.class.name, bookmarkable_id:quote.id}}, method: :post%>
                <% end %>
            </div>
        <% end %>
        </div>
    <% end %>
    <% if user_signed_in? %>
        <%= button_to "Log out", destroy_user_session_path, method: :delete %>
    <% end %>
</div>

